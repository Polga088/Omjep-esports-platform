/**
 * Client Prisma + surface générée (dont les enums comme `UserRole`).
 * Il n’y a pas d’export nommé manuel ici : tout vient du client généré.
 * Côté front / enums partagés uniquement, préférer `@omjep/shared` (réexport ciblé).
 */
export * from './generated/prisma';
export * from './client';
